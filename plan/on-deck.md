# On deck — the only place Focus tasks come from

Max 3 candidates. Refreshed at the weekly review. The morning drill picks
from THIS list, never from the backlog. Everything else → parking lot.

1. Finish grading SCI home assignments (started, AI-assisted).
2. Deploy the tracker bot (BotFather token → .env → docker compose up).
3. Next AI lecture prep (previous one delivered 2026-07-10).
